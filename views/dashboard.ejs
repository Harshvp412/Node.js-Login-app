<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes</title> 
    <link rel="stylesheet"href="https://bootswatch.com/4/cyborg/bootstrap.min.css" />
</head>
<body>
<h1 class="mt-4">Dashboard</h1>
<p class="lead mb-3">Welcome <%= name %> </p>
<a  href="/dashboard/add" class="btn btn-secondary">Add new Note</a>
  <a style="margin:5px" href="/users/logout" class="btn btn-secondary">Logout</a>
 
<% if(items.length >0) { %>
  <div class="row mt-5">
    <div class="col -8 -auto">
      <div class="card card-body">
        
        <form >
          <div class="form-group">
           <% for (var i=0; i<items.length ; i++) { %>
            <% if( userID == items[i].notesID){ %>  
            <h2 class="text- mb-8"><i class="bi bi-file-text"></i>  Note: </h2>
                <ul>
                  <li> <b> Title : </b> <%= items[i].title %></li>
                  <li> <b> Text : </b> <%= items[i].text %></li> 
                  
               </ul>
               <a  href="/dashboard/edit/<%= items[i]._id %>" class="btn btn-secondary">Edit</a>
               <a style="margin:5px" href="/delete/<%= items[i]._id %>" class="btn btn-secondary">Delete</a>
            <% } %> 
            <% } %> 
            </div>
         
        </form>
      </div>
    </div>
  </div>
<% } else { %>
    <br/> <br/> No Note Found  
    <% } %>     
</body>
</html>
